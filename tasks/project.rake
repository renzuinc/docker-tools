namespace :project do
  desc "Generate `.rubocop.yml`, modify `Rakefile`, etc.  Specify"\
    " TEMPLATE, or omit for a list of available options."
  task :init do
    # TODO: Generate above-listed files.
  end
end
